version: '3.9'

services:
  app:
    build:
      context: ./app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=development
    volumes:
      - ./app/src:/app/src
    command: ["npx", "nodemon", "src/index.js"]
    restart: unless-stopped
